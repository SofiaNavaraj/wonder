package er.directtoweb.bootstrap.components;

import com.webobjects.appserver.WOContext;
import er.extensions.components.ERXCheckboxMatrix;

public class ERBCheckboxMatrix extends ERXCheckboxMatrix {
    public ERBCheckboxMatrix(WOContext context) {
        super(context);
    }
}