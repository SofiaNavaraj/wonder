package sa.directtoweb.components.numbers;

import com.webobjects.appserver.WOContext;
import er.directtoweb.components.numbers.ERD2WEditNumber;

public class SAD2WEditNumber extends ERD2WEditNumber {
    public SAD2WEditNumber(WOContext context) {
        super(context);
    }
}